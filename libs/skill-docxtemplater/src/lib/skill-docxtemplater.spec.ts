import { skillDocxtemplater } from './skill-docxtemplater';

describe('skillDocxtemplater', () => {
  it('should work', () => {
    expect(skillDocxtemplater()).toEqual('skill-docxtemplater');
  });
});
