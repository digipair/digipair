import { skillWebJsoneditor } from './skill-web-jsoneditor';

describe('skillWebJsoneditor', () => {
  it('should work', () => {
    expect(skillWebJsoneditor()).toEqual('skill-web-jsoneditor');
  });
});
