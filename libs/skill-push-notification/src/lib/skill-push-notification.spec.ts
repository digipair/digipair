import { skillPushNotification } from './skill-push-notification';

describe('skillPushNotification', () => {
  it('should work', () => {
    expect(skillPushNotification()).toEqual('skill-push-notification');
  });
});
