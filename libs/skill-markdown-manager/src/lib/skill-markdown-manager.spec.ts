import { skillMarkdownManager } from './skill-markdown-manager';

describe('skillMarkdownManager', () => {
  it('should work', () => {
    expect(skillMarkdownManager()).toEqual('skill-markdown-manager');
  });
});
