import { skillDataManagement } from './skill-data-management';

describe('skillDataManagement', () => {
  it('should work', () => {
    expect(skillDataManagement()).toEqual('skill-data-management');
  });
});
